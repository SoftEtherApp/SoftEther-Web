/* ════════════════════════════════════
   Login — auth entry point (scaffold)
   ════════════════════════════════════ */

import { type JSX } from "react";

export default function LoginPage(): JSX.Element {
	return (
		<div>
			<h1 className="m-0 mb-sm fs-lg fw-700 text-primary">Sign in</h1>
			<p className="m-0 text-muted">Authentication is not wired up yet.</p>
		</div>
	);
}
