/* ════════════════════════════════════
   Settings — admin settings (scaffold)
   ════════════════════════════════════ */

import { type JSX } from "react";

export default function SettingsPage(): JSX.Element {
	return (
		<section className="py-2xl px-lg sm:py-2xl sm:px-md">
			<div className="m-auto mw-1040">
				<h1 className="mb-sm text-center fw-700 fs-lg text-primary">Settings</h1>
				<p className="m-auto mb-2xl text-center text-secondary mw-540 fs-base">
					Admin settings are not wired up yet.
				</p>
			</div>
		</section>
	);
}
